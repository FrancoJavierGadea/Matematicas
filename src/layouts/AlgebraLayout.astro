---
import Layout from "@layouts/Layout.astro";

const sideBarLinks = [
    {
        name: 'Teoria',
        children: [
            {name: 'Regla de Ruffini', url: '/algebra/ruffini'}
        ]
    },
]

const { title } = Astro.props;
---


<Layout title={title} showSidebar sideBarTitle="Algebra" sideBarLinks={sideBarLinks}>

    <slot />

</Layout>